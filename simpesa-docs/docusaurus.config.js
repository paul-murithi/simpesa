// @ts-check

/** @type {import('@docusaurus/types').Config} */
const config = {
  title: 'Sim-Pesa',
  tagline: 'Local-first M-Pesa API Simulator',
  // favicon: 'img/favicon.ico', // Disabled until asset is provided

  url: 'https://simpesa-docs.vercel.app',
  baseUrl: '/',

  organizationName: 'simpesa', 
  projectName: 'simpesa',

  onBrokenLinks: 'throw',
  onBrokenMarkdownLinks: 'warn',

  i18n: {
    defaultLocale: 'en',
    locales: ['en'],
  },

  presets: [
    [
      'classic',
      /** @type {import('@docusaurus/preset-classic').Options} */
      ({
        docs: {
          sidebarPath: require.resolve('./sidebars.js'),
          // The default is 'docs', keeping it isolated from the landing page
          routeBasePath: 'docs', 
        },
        blog: false,
        theme: {
          customCss: require.resolve('./src/css/custom.css'),
        },
      }),
    ],
  ],

  themeConfig:
    /** @type {import('@docusaurus/preset-classic').ThemeConfig} */
    ({
      navbar: {
        title: 'Sim-Pesa',
        items: [
          {
            type: 'docSidebar',
            sidebarId: 'tutorialSidebar',
            position: 'left',
            label: 'Documentation',
          },
          {
            href: 'https://github.com/simpesa/simpesa',
            label: 'GitHub',
            position: 'right',
          },
        ],
      },
      footer: {
        style: 'dark',
        copyright: `Copyright © ${new Date().getFullYear()} Sim-Pesa. Built with Docusaurus.`,
      },
      prism: {
        theme: require('prism-react-renderer').themes.github,
        darkTheme: require('prism-react-renderer').themes.dracula,
      },
    }),
};

module.exports = config;
